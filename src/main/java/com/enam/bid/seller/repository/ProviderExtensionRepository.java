package com.enam.bid.seller.repository;

import com.enam.bid.seller.entity.bidseller.ProviderExtension;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProviderExtensionRepository extends JpaRepository<ProviderExtension, Integer> {
}